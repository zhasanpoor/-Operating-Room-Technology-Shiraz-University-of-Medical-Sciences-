# ✅ چک‌لیست پروژه — سایت تکنولوژی اتاق عمل

> این فایل حافظهٔ پروژه است. هر آیتم که کامل و **تست** شد تیک می‌خورد.
> در هر گفتگوی جدید: اول این فایل خوانده شود، سپس اولین آیتم بدون تیک ادامه یابد.
>
> وضعیت‌ها: `[ ]` انجام نشده · `[~]` نیمه‌کاره · `[x]` کامل و تست‌شده

---

## فاز ۰ — پایه و زیرساخت

- [x] راه‌اندازی Express + SQLite (sql.js) + ساختار پوشه‌ها
- [x] جدول‌های categories / operations / operation_content
- [x] seed اولیه محتوای عمل‌های ۱ تا ۱۸
- [x] لاگین ساده با bcrypt + JWT
- [x] **مهاجرت (migration) اسکیمای دیتابیس به نسخه ۲** — `migrations.js`، ۷ مهاجرت اجرا شد،
      هر ۱۴۳ عمل و ۱۰ دسته‌بندی سالم ماندند (پشتیبان: `database.sqlite.bak-*`)
- [x] رفع `JWT_SECRET` پیش‌فرض ناامن — `config.js`: در production بدون secret قوی
      سرور بالا نمی‌آید؛ در توسعه secret تصادفی در `.jwt-secret` ذخیره می‌شود
- [x] فایل `.env.example` + مستندسازی متغیرها
- [x] ماژول تاریخ شمسی `public/js/jalali.js` (مشترک سرور و مرورگر) — ۷ تاریخ مرجع
      و ۵۰۰۰ رفت‌وبرگشت تست شد (`npm run test:dates`)

## فاز ۰.۵ — پایداری داده روی سرور (بحرانی) 🔴

> **مشکل:** فایل‌سیستم Render موقتی است. `database.sqlite` با هر دیپلوی پاک
> می‌شود، پس هر کاربر ثبت‌نام‌کرده و هر پست نویسنده از بین می‌رود.
> **تصمیم کاربر:** مهاجرت به Postgres رایگان Render.

- [x] لایهٔ دسترسی ناهمگام `db/index.js` — Postgres روی سرور، SQLite در توسعه.
      تبدیل خودکار `?` به `$n`، تراکنش، rollback. ۱۸ تست (`npm run test:db`)
- [ ] **ساخت دیتابیس Postgres در داشبورد Render و اتصالش به سرویس** ← کار شما
- [ ] بازنویسی اسکیما و مهاجرت‌ها به‌صورت سازگار با هر دو گویش
- [ ] بازنویسی `seed.js` و `seed-content.js` به شکل ناهمگام
- [ ] بازنویسی `routes/api.js` به شکل ناهمگام (هم‌زمان با فاز ۲ انجام می‌شود)
- [ ] انتقال محتوای فعلی (۱۴۳ عمل، ۱۰ دسته‌بندی) به Postgres
- [ ] پایداری فایل‌های آپلودی (`uploads/` هم با دیپلوی پاک می‌شود)

## فاز ۱ — کاربران و سطوح دسترسی

> ۳۷ تست یکپارچه: `npm run test:accounts`

- [x] ثبت‌نام عمومی کاربر — اعتبارسنجی نام کاربری، رمز قوی (حداقل ۸ کاراکتر،
      حرف+عدد، رد رمزهای رایج و رمز حاوی نام کاربری)، ایمیل یکتا
- [x] پروفایل کاربر: ویرایش نام، ایمیل، بیو (با پاک‌سازی XSS)
- [x] آپلود عکس پروفایل — سقف ۲ مگابایت، فقط JPG/PNG/WebP، و **بررسی امضای
      واقعی فایل (magic bytes)** نه فقط پسوند؛ فایل جعلی حذف و به‌عنوان
      رویداد امنیتی ثبت می‌شود. عکس قبلی هم از دیسک پاک می‌شود.
- [x] تغییر رمز عبور با تأیید رمز فعلی
- [x] درخواست ارتقا به «نویسنده» از سمت کاربر
- [x] تأیید / رد درخواست نویسندگی توسط ادمین + اعلان به کاربر
- [x] فعال / غیرفعال‌سازی کاربر — توکن قبلیِ کاربر غیرفعال بلافاصله رد می‌شود
- [x] تغییر سطح دسترسی کاربر توسط ادمین
- [x] حفظ داده‌های قدیمی هنگام تنزل و ارتقای مجدد سطح — امتیاز و پست‌ها
      دست‌نخورده می‌مانند؛ هنگام حذف کاربر هم پست‌ها فقط بی‌صاحب می‌شوند
- [x] محافظت از آخرین مدیر: حذف / غیرفعال‌سازی / تنزل آخرین ادمین ممکن نیست
- [ ] رابط کاربری ثبت‌نام و پروفایل در سایت *(سمت سرور کامل است)*
- [x] 🔴 **رفع باگ ارتقای سطح دسترسی** — `adminOnly` قبلی نقش `editor` را قبول
      می‌کرد و روی مسیر ساخت کاربر بود؛ یعنی نویسنده می‌توانست حساب ادمین
      بسازد و کاربر حذف کند. حالا `requireAdmin` / `requireAuthor` جدا شدند.
- [x] نقش و وضعیت کاربر در هر درخواست از دیتابیس خوانده می‌شود نه از توکن —
      تنزل سطح یا غیرفعال‌سازی بلافاصله اثر می‌کند، نه بعد از انقضای توکن

## فاز ۲ — گردش کار محتوا (Workflow)

> بک‌اند کامل شد — ۲۱ تست (`npm run test:permissions`). رابط کاربری هنوز مانده.

- [x] ستون‌های `author_id`, `status`, `published_at` و... (مهاجرت ۰۰۳)
- [x] وضعیت‌ها: پیش‌نویس / در انتظار تأیید / تأییدشده / رد شده / نیازمند اصلاح
- [x] نویسنده فقط پست‌های **خودش** را ویرایش می‌کند (`checkOperationAccess`)
- [x] ارسال پست برای بررسی — `POST /operations/:id/submit`
- [x] صف انتظار تأیید — `GET /review-queue` (فقط مدیر)
- [x] تأیید / رد / درخواست اصلاح — `POST /operations/:id/review` + کامنت اختیاری
- [x] **قفل شدن پست پس از تأیید** — حتی نویسنده و حتی مدیر (مگر با unlock عمدی)
- [x] باز کردن قفل توسط مدیر — `POST /operations/:id/unlock` + ثبت در لاگ ممیزی
- [x] اعلان برای نویسنده هنگام تأیید/رد/درخواست اصلاح (در جدول notifications)
- [x] رفع ناسازگاری: مهاجرت ۰۰۹ — محتوای seed شده دیگر قفل نیست (متعلق به
      مدیر است و قفل بودنش یعنی ۱۴۳ عمل غیرقابل ویرایش می‌شدند)
- [x] **رابط کاربری صف بررسی** در پنل مدیر — کارت پست با نام نویسنده، دسته،
      زمان نسبی شمسی، و دکمه‌های مشاهده/تأیید/اصلاح/رد (تست چشمی شد)
- [x] چرخهٔ تأیید از UI: تأیید → حذف از صف → انتشار + قفل (تست چشمی end-to-end)
- [x] مسیر API کامنت دوطرفه ساخته شد (`GET/POST /operations/:id/comments`)
- [ ] نمایش کامنت‌ها و دکمهٔ ارسال برای نویسنده در UI *(مسیر API آماده)*
- [ ] نمایش نام نویسنده + تاریخ ایجاد به **شمسی** روی هر پست در سایت عمومی

## فاز ۳ — امنیت

- [x] 🔴 **رفع رمز پیش‌فرض `admin/admin123`** — این رمز در `seed.js` ثابت بود و چون
      seed با هر دیپلوی روی Render دوباره اجرا می‌شود، پنل مدیریت سایت عمومی
      همیشه با یک رمز شناخته‌شده باز بود. حالا رمز تصادفی ساخته و یک بار در
      لاگ چاپ می‌شود؛ با `ADMIN_PASSWORD` قابل تعیین است. حساب نمایشی
      `editor/editor123` دیگر در production ساخته نمی‌شود.
- [ ] 🟠 **ارتقای `multer` از ۱.x به ۲.x** — نسخهٔ فعلی آسیب‌پذیری شناخته‌شده دارد
      (هشدار npm هنگام نصب). هم‌زمان با بازنویسی مسیر آپلود انجام شود.
- [ ] اجبار به تغییر رمز مدیر در اولین ورود
- [ ] هش رمز با bcrypt (cost ≥ ۱۲)
- [x] پس از ۳ تلاش ناموفق → کپچای ساده فارسی (جمع دو عدد، امضاشده با HMAC،
      بدون نیاز به session سمت سرور)
- [x] پس از ۵ تلاش ناموفق → بلاک کاربر **و** IP به مدت ۱۵ دقیقه
- [x] محافظت brute-force با ردیابی هم‌زمان IP و نام کاربری — ردیابی فقط با IP
      کاربران بی‌گناه پشت NAT را بلاک می‌کند، و فقط با نام کاربری جلوی
      امتحان کردن هزاران نام از یک IP را نمی‌گیرد
- [x] رفع باگ: کپچای غلط تلاش ناموفق حساب نمی‌شد، پس شمارنده روی ۳ قفل
      می‌ماند و بلاکِ ۵ تلاش هرگز فعال نمی‌شد
- [x] پیام یکسان برای «کاربر وجود ندارد» و «رمز غلط» تا نام کاربری لو نرود
- [ ] محدودیت نرخ عمومی روی کل API (الان ۱۰۰۰ در ۱۵ دقیقه، خیلی زیاد است)
- [x] پاک‌سازی (sanitize) تمام ورودی‌های HTML — `lib/sanitize.js` با کتابخانهٔ
      معتبر `sanitize-html` و سیاست allowlist *(ماژول آماده؛ اتصال به API در فاز بعد)*
- [x] اسکن متن و لینک‌های ارسالی برای الگوهای حمله — شامل تشخیص دور زدن با
      URL-encode و HTML entity؛ ۴۵ تست بدون خطای مثبت کاذب (`npm run test:security`)
- [x] فقط لینک یوتیوب/آپارات/ویمئو معتبر پذیرفته شود — سفیدفهرست دامنه + ساخت
      نشانی embed بدون کوکی؛ لینک‌های SSRF (localhost, 169.254.169.254) رد می‌شوند
- [x] **اتصال لایهٔ پاک‌سازی به مسیر ذخیرهٔ محتوا** — HTML ویرایشگر پاک می‌شود،
      عنوان‌ها به متن ساده، لینک ویدیو فقط از دامنه‌های مجاز. ۱۱ تست یکپارچه
      (`npm run test:api`). ثبت رویداد در `security_events` هم تأیید شد.
- [x] رفع نشتی: `GET /files` فهرست تمام فایل‌های همهٔ کاربران را به هر
      کاربر واردشده می‌داد؛ حالا نویسنده فقط فایل‌های خودش را می‌بیند
- [ ] اتصال پاک‌سازی به بقیهٔ مسیرها (کاربران، دسته‌بندی، آپلود)
- [ ] در صورت شناسایی حمله: گزارش به ادمین + غیرفعال‌سازی موقت کاربر
- [ ] اعتبارسنجی سخت‌گیرانه آپلود: نوع MIME واقعی، حجم، پسوند، نام امن
- [ ] فعال‌سازی CSP در helmet (الان خاموش است) + سایر هدرهای امنیتی
- [ ] محدود کردن CORS به دامنه‌های مجاز
- [ ] کوکی امن: httpOnly + secure + sameSite
- [ ] محافظت CSRF روی درخواست‌های تغییردهنده
- [x] لاگ امنیتی (audit log) رویدادهای حساس — تأیید/رد پست، تغییر نقش،
      فعال/غیرفعال‌سازی، تغییر رمز، حذف کاربر
- [x] اعتبارسنجی سخت‌گیرانه آپلود آواتار: نوع MIME، حجم، و امضای واقعی فایل

## فاز ۴ — رابط کاربری و تجربه کاربری

- [x] **ویرایشگر متن غنی** — Quill با نوار ابزار فارسی، راست‌به‌چپ، تیتر، لیست،
      نقل‌قول، جدول، رنگ، پیوند و **درج تصویر داخل متن** (به‌جای متن
      `[تصویر: ...]` قبلی که واقعاً تصویر نبود). به‌صورت محلی سرو می‌شود
      چون دسترسی به CDN خارجی از ایران ناپایدار است.
- [x] رفع باگ: نوشتن‌های `prepare().run()` هرگز روی دیسک ذخیره نمی‌شدند —
      هر ویرایش محتوا با ری‌استارت سرویس بی‌صدا از بین می‌رفت
- [x] رفع باگ: `undefined` در sql.js خطای بدون پیام می‌داد (ذخیرهٔ جزئی محتوا ۵۰۰ می‌شد)
- [ ] نمایش صحیح محتوای HTML جدید در سایت عمومی *(کد نوشته شد، نیاز به تست چشمی)*

- [x] بازطراحی داشبورد ادمین و نویسنده — کارت آمار با آیکون و شمارش انیمیشنی،
      نمودار میله‌ای پراکندگی دسته‌ها، فعالیت‌های اخیر (تست چشمی شد)
- [x] پنهان شدن بخش‌های مخصوص مدیر برای نویسنده (`.admin-only`)
- [ ] نمایش ویدیو یوتیوب به صورت embed داخل خود سایت
- [ ] جستجو و فیلتر پیشرفتهٔ عمل‌ها
- [ ] طراحی واکنش‌گرا (موبایل / تبلت / دسکتاپ)
- [ ] حالت شب (dark mode)
- [ ] صفحات خطای اختصاصی ۴۰۴ و ۵۰۰ به فارسی
- [ ] حالت‌های خالی (empty state) و اسکلت بارگذاری (skeleton loader)
- [ ] تمام پیام‌های خطا و هشدار: فارسی، خلاصه، دوستانه و جوان‌پسند
- [ ] راهنمای استفاده مخصوص هر سطح کاربری
- [ ] پاپ‌آپ خوش‌آمدگویی و تور راهنما برای کاربر تازه‌وارد
- [ ] دسترس‌پذیری (a11y): کنتراست، ناوبری با کیبورد، برچسب‌های ARIA

## فاز ۵ — داشبورد و انگیزش نویسندگان

- [x] داشبورد گرافیکی نویسنده با نمودار و انیمیشن (شمارش انیمیشنی + نوار پیشرفت)
- [x] شمارش فعالیت‌ها: تعداد پست، تأییدشده، در انتظار، پیش‌نویس، بازدید
- [x] سطح‌بندی نویسندگان (تازه‌کار / برنزی / نقره‌ای / طلایی / الماسی) با نشان
- [x] پیام‌های تشویقی و «چند پست تا سطح بعد» (تست چشمی شد)
- [ ] جدول رتبه‌بندی نویسندگان فعال (رقابت بین نویسندگان)
- [x] گزارش‌های مدیریتی در داشبورد ادمین (آمار وضعیت، پراکندگی، فعالیت اخیر)

## فاز ۶ — تحویل نهایی

- [ ] `README.md` کامل: نصب، اجرا، معماری، سطوح دسترسی، امنیت، استقرار
- [ ] راهنمای استقرار روی Render
- [ ] تست دستی تمام مسیرهای اصلی هر سه نقش
- [ ] بررسی نهایی امنیتی
- [ ] پاک‌سازی کد مرده و فایل‌های اضافی (`data.js`, `extracted_content.txt`, `1-12.md` و ...)

---

## 📌 یادداشت‌های فنی

- **پایگاه داده:** `sql.js` عمداً انتخاب شده (کامیت `8dc756a`) چون `better-sqlite3` روی Render کامپایل نمی‌شد. تغییرش ندهید مگر با دلیل.
- **هشدار عملکرد:** `sql.js` در هر نوشتن کل فایل دیتابیس را روی دیسک بازنویسی می‌کند. برای این مقیاس مشکلی نیست.
- **نقش `editor`** در دیتابیس فعلی همان «نویسنده» است.
- تاریخ‌ها در دیتابیس **میلادی (UTC)** ذخیره می‌شوند و فقط هنگام نمایش شمسی می‌شوند.
