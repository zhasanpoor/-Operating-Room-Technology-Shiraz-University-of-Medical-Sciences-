# ✅ چک‌لیست پروژه — سایت تکنولوژی اتاق عمل

> این فایل حافظهٔ پروژه است. هر آیتم که کامل و **تست** شد تیک می‌خورد.
> در هر گفتگوی جدید: اول این فایل خوانده شود، سپس اولین آیتم بدون تیک ادامه یابد.
>
> وضعیت‌ها: `[ ]` انجام نشده · `[~]` نیمه‌کاره · `[x]` کامل و تست‌شده

---

## فاز ۰ — پایه و زیرساخت

- [x] راه‌اندازی Express + SQLite (sql.js) + ساختار پوشه‌ها
- [x] جدول‌های categories / operations / operation_content
- [x] seed اولیه محتوای عمل‌های ۱ تا ۱۸
- [x] لاگین ساده با bcrypt + JWT
- [x] **مهاجرت (migration) اسکیمای دیتابیس به نسخه ۲** — `migrations.js`، ۷ مهاجرت اجرا شد،
      هر ۱۴۳ عمل و ۱۰ دسته‌بندی سالم ماندند (پشتیبان: `database.sqlite.bak-*`)
- [x] رفع `JWT_SECRET` پیش‌فرض ناامن — `config.js`: در production بدون secret قوی
      سرور بالا نمی‌آید؛ در توسعه secret تصادفی در `.jwt-secret` ذخیره می‌شود
- [x] فایل `.env.example` + مستندسازی متغیرها
- [x] ماژول تاریخ شمسی `public/js/jalali.js` (مشترک سرور و مرورگر) — ۷ تاریخ مرجع
      و ۵۰۰۰ رفت‌وبرگشت تست شد (`npm run test:dates`)

## فاز ۰.۵ — پایداری داده روی سرور (بحرانی) 🔴

> **مشکل:** فایل‌سیستم Render موقتی است. `database.sqlite` با هر دیپلوی پاک
> می‌شود، پس هر کاربر ثبت‌نام‌کرده و هر پست نویسنده از بین می‌رود.
> **تصمیم کاربر:** مهاجرت به Postgres رایگان Render.

- [x] لایهٔ دسترسی ناهمگام `db/index.js` — Postgres روی سرور، SQLite در توسعه.
      تبدیل خودکار `?` به `$n`، تراکنش، rollback. ۱۸ تست (`npm run test:db`)
- [ ] **ساخت دیتابیس Postgres در داشبورد Render و اتصالش به سرویس** ← کار شما
- [ ] بازنویسی اسکیما و مهاجرت‌ها به‌صورت سازگار با هر دو گویش
- [ ] بازنویسی `seed.js` و `seed-content.js` به شکل ناهمگام
- [ ] بازنویسی `routes/api.js` به شکل ناهمگام (هم‌زمان با فاز ۲ انجام می‌شود)
- [ ] انتقال محتوای فعلی (۱۴۳ عمل، ۱۰ دسته‌بندی) به Postgres
- [ ] پایداری فایل‌های آپلودی (`uploads/` هم با دیپلوی پاک می‌شود)

## فاز ۱ — کاربران و سطوح دسترسی

- [ ] ثبت‌نام عمومی کاربر معمولی (با اعتبارسنجی قوی رمز)
- [ ] پروفایل کاربر: ویرایش نام، نام کاربری، ایمیل، بیو
- [ ] آپلود و برش عکس پروفایل (آواتار) با محدودیت حجم و نوع
- [ ] تغییر رمز عبور (با تأیید رمز فعلی)
- [ ] درخواست ارتقا به «نویسنده» از سمت کاربر
- [ ] تأیید / رد درخواست نویسندگی توسط ادمین
- [ ] فعال / غیرفعال‌سازی کاربر توسط ادمین
- [ ] تغییر سطح دسترسی کاربر توسط ادمین
- [ ] حفظ داده‌های قدیمی هنگام تنزل و ارتقای مجدد سطح (پست‌ها و آمار برنگردند به صفر)
- [ ] رفع باگ امنیتی فعلی: `adminOnly` به نقش `editor` اجازهٔ حذف کاربر می‌دهد

## فاز ۲ — گردش کار محتوا (Workflow)

- [ ] افزودن ستون‌های `author_id`, `status`, `created_at`, `published_at` به عمل‌ها
- [ ] وضعیت‌ها: پیش‌نویس / در انتظار تأیید / تأییدشده / رد شده / نیازمند اصلاح
- [ ] نویسنده فقط پست‌های **خودش** را می‌بیند و ویرایش می‌کند
- [ ] ارسال پست برای بررسی توسط نویسنده
- [ ] صف انتظار تأیید در پنل ادمین
- [ ] تأیید / رد / درخواست اصلاح توسط ادمین
- [ ] سیستم کامنت (گفتگوی ادمین ↔ نویسنده) روی هر پست
- [ ] **قفل شدن پست پس از تأیید** — حتی نویسنده هم نتواند ویرایش کند
- [ ] نمایش نام نویسنده + تاریخ ایجاد به **شمسی** روی هر پست
- [ ] اعلان (notification) برای نویسنده هنگام تغییر وضعیت پست

## فاز ۳ — امنیت

- [ ] هش رمز با bcrypt (cost ≥ ۱۲)
- [ ] محدودیت نرخ درخواست واقعی روی لاگین/ثبت‌نام (نه ۱۰۰۰ تا)
- [ ] پس از ۳ تلاش ناموفق → کپچای ساده
- [ ] پس از ۵ تلاش ناموفق → بلاک کاربر و IP به مدت ۱۵ دقیقه
- [ ] محافظت brute-force با ردیابی هم‌زمان IP و نام کاربری
- [x] پاک‌سازی (sanitize) تمام ورودی‌های HTML — `lib/sanitize.js` با کتابخانهٔ
      معتبر `sanitize-html` و سیاست allowlist *(ماژول آماده؛ اتصال به API در فاز بعد)*
- [x] اسکن متن و لینک‌های ارسالی برای الگوهای حمله — شامل تشخیص دور زدن با
      URL-encode و HTML entity؛ ۴۵ تست بدون خطای مثبت کاذب (`npm run test:security`)
- [x] فقط لینک یوتیوب/آپارات/ویمئو معتبر پذیرفته شود — سفیدفهرست دامنه + ساخت
      نشانی embed بدون کوکی؛ لینک‌های SSRF (localhost, 169.254.169.254) رد می‌شوند
- [ ] **اتصال لایهٔ پاک‌سازی به مسیرهای API** ← اولین کار فاز بعد
- [ ] در صورت شناسایی حمله: گزارش به ادمین + غیرفعال‌سازی موقت کاربر
- [ ] اعتبارسنجی سخت‌گیرانه آپلود: نوع MIME واقعی، حجم، پسوند، نام امن
- [ ] فعال‌سازی CSP در helmet (الان خاموش است) + سایر هدرهای امنیتی
- [ ] محدود کردن CORS به دامنه‌های مجاز
- [ ] کوکی امن: httpOnly + secure + sameSite
- [ ] محافظت CSRF روی درخواست‌های تغییردهنده
- [ ] لاگ امنیتی (audit log) رویدادهای حساس

## فاز ۴ — رابط کاربری و تجربه کاربری

- [ ] بازطراحی داشبورد با سه نمای مجزا (ادمین / نویسنده / کاربر)
- [ ] نمایش ویدیو یوتیوب به صورت embed داخل خود سایت
- [ ] جستجو و فیلتر پیشرفتهٔ عمل‌ها
- [ ] طراحی واکنش‌گرا (موبایل / تبلت / دسکتاپ)
- [ ] حالت شب (dark mode)
- [ ] صفحات خطای اختصاصی ۴۰۴ و ۵۰۰ به فارسی
- [ ] حالت‌های خالی (empty state) و اسکلت بارگذاری (skeleton loader)
- [ ] تمام پیام‌های خطا و هشدار: فارسی، خلاصه، دوستانه و جوان‌پسند
- [ ] راهنمای استفاده مخصوص هر سطح کاربری
- [ ] پاپ‌آپ خوش‌آمدگویی و تور راهنما برای کاربر تازه‌وارد
- [ ] دسترس‌پذیری (a11y): کنتراست، ناوبری با کیبورد، برچسب‌های ARIA

## فاز ۵ — داشبورد و انگیزش نویسندگان

- [ ] داشبورد گرافیکی نویسنده با نمودار و انیمیشن
- [ ] شمارش فعالیت‌ها: تعداد پست، تأییدشده، در انتظار، بازدید
- [ ] سطح‌بندی نویسندگان (برنزی / نقره‌ای / طلایی / الماسی)
- [ ] نشان‌ها (badge) و دستاوردها
- [ ] جدول رتبه‌بندی نویسندگان فعال
- [ ] پیام‌های تشویقی («شما سومین نویسندهٔ فعال ما هستید!»)
- [ ] گزارش‌های مدیریتی در داشبورد ادمین (کاربران، پست‌ها، روند رشد)

## فاز ۶ — تحویل نهایی

- [ ] `README.md` کامل: نصب، اجرا، معماری، سطوح دسترسی، امنیت، استقرار
- [ ] راهنمای استقرار روی Render
- [ ] تست دستی تمام مسیرهای اصلی هر سه نقش
- [ ] بررسی نهایی امنیتی
- [ ] پاک‌سازی کد مرده و فایل‌های اضافی (`data.js`, `extracted_content.txt`, `1-12.md` و ...)

---

## 📌 یادداشت‌های فنی

- **پایگاه داده:** `sql.js` عمداً انتخاب شده (کامیت `8dc756a`) چون `better-sqlite3` روی Render کامپایل نمی‌شد. تغییرش ندهید مگر با دلیل.
- **هشدار عملکرد:** `sql.js` در هر نوشتن کل فایل دیتابیس را روی دیسک بازنویسی می‌کند. برای این مقیاس مشکلی نیست.
- **نقش `editor`** در دیتابیس فعلی همان «نویسنده» است.
- تاریخ‌ها در دیتابیس **میلادی (UTC)** ذخیره می‌شوند و فقط هنگام نمایش شمسی می‌شوند.
